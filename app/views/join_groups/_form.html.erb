<div class="col ml-3">
  <%= simple_form_for([@group, @group.join_groups.build]) do |f| %>
    <div class="row input-group mb-3 comment_box">
      <% if current_user.Admin? %>
        <%= f.select :user_id, User.artist.map { |r| [r.name, r.id,] }, include_blank: true %>
      <% end %>

      <%= f.input :username, label: "Username" %>
      <div class="input-group">
        <%= f.button :submit, "Send", class: "btn btn-outline-secondary comment_button"  %>
      </div>
      <div class="input-group">
          tes buat error
      </div>
    </div>
  <% end %>
</div>